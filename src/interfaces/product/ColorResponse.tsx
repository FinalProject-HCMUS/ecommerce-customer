export interface ColorResponse {
  id: string;
  name: string;
  code: string;
  createdAt: string;
  createdBy: string;
  updatedAt: string;
  updatedBy: string;
}

export interface ColorProps {
  id: string;
  name: string;
  colorCode: string;
}
